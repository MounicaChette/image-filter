name: test-workflow


# Manual trigger with parameters
on:
  workflow_dispatch
  
jobs:

  Build:
    runs-on: windows-latest

    # Checkout code
    steps:
    - name: display environments 
      uses: actions/checkout@v2
      
   
        
    - name: display stage variables
      run: echo ${{vars.variable1}}, ${{env.variable1}}
